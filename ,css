body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    background-color: #1a1a1a;
    font-family: 'Segoe UI', sans-serif;
    overflow: hidden; /* Prevents scrolling while playing */
    touch-action: none; /* Critical: stops the browser from bouncing/scrolling */
}

.game-container {
    text-align: center;
    color: #fff;
}

canvas {
    border: 4px solid #333;
    background-color: #000;
    max-width: 90vw;
    max-height: 70vh;
    box-shadow: 0 0 20px rgba(0,0,0,0.5);
}

.score-board {
    font-size: 24px;
    margin-bottom: 10px;
}

.instructions {
    font-size: 14px;
    color: #888;
    margin: 10px 0;
}

#restartBtn {
    background: #27ae60;
    color: white;
    border: none;
    padding: 12px 24px;
    font-size: 16px;
    border-radius: 5px;
    cursor: pointer;
}
